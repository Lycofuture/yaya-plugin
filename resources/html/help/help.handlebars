{{extend defaultLayout}}
{{block 'main'}}
<div class="info-box">
    <div class="info-title">{{helpCfg.title}}</div>
    <div class="info-label">{{helpCfg.subTitle}}</div>
</div>
{{each helpList group}}
<div class="cont-box">
    <div class="help-group">{{group.group}}</div>
<div class="help-table">
{{each group.list help}}
    <div class="item">
        <strong class="title">{{help.title}}</strong>
        <span class="desc">{{help.desc}}</span>
    </div>
{{/each}}
</div>
</div>
{{/each}}
<div class="copyright">{{@sys.copyright}}</div>
{{/block}}
